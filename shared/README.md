# Shared Types

This directory contains shared TypeScript types that can be used by both frontend and backend (if using TypeScript for backend validation schemas).

## Structure

- `types.ts` - Common type definitions for resources, packets, etc.

